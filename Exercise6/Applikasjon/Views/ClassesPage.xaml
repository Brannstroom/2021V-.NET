<Page
    x:Class="Applikasjon.Views.ClassesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <TextBlock HorizontalAlignment="Left" Margin="612,47,0,0" Text="Classes" TextWrapping="Wrap" VerticalAlignment="Top" Height="153" Width="258" FontSize="72"/>
            <Button x:Name="ClassesButton"
                    Content="Show list"
                    Margin="0,243,0,0"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Click="ClassesButton_Click"
                    ClickMode="Press"
                    />
            <TextBox x:Name="ClassesList" HorizontalAlignment="Center" Text="" TextWrapping="Wrap" VerticalAlignment="Top" Height="266" Width="580" Margin="0,345,0,0"/>
            <!--
                The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.
            -->
        </Grid>
    </Grid>
</Page>
